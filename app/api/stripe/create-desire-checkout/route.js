import {NextResponse} from 'next/server'
import {cookies} from 'next/headers'
import {readClubToken,clubCookieName} from '../../../../lib/clubAuth'
const PRICES={'my-dvaja':'price_1UCg74PeOVi2qayNxUwuStOL','bez-filtra':'price_1UCg7APeOVi2qayNcNdHMn1R','nasa-buducnost':'price_1UCg7GPeOVi2qayNEZQhwKnp','intimita':'price_1UCg7MPeOVi2qayNIjVF1Bb1','tajne-tuzby':'price_1UCg7SPeOVi2qayNQFr3J0HQ'}
export async function GET(req){
 try{const m=readClubToken(cookies().get(clubCookieName)?.value);if(!m?.email)return NextResponse.redirect(new URL('/login',req.url));const url=new URL(req.url);const bundle=url.searchParams.get('bundle')==='1';const slug=url.searchParams.get('test');const price=PRICES[slug];if(!bundle&&!price)return NextResponse.json({error:'Unknown test'},{status:400});if(!process.env.STRIPE_SECRET_KEY)return NextResponse.json({error:'Stripe server key missing'},{status:500});const origin=(process.env.NEXT_PUBLIC_SITE_URL||new URL(req.url).origin).replace(/\/$/,'');const form=new URLSearchParams();form.set('mode','payment');form.set('success_url',bundle?`${origin}/desire?bundle_purchased=1&session_id={CHECKOUT_SESSION_ID}`:`${origin}/desire/deep/${slug}?purchased=1&session_id={CHECKOUT_SESSION_ID}`);form.set('cancel_url',bundle?`${origin}/desire?cancelled=1`:`${origin}/desire/deep/${slug}?cancelled=1`);form.set('customer_email',m.email);if(bundle){form.set('line_items[0][price_data][currency]','eur');form.set('line_items[0][price_data][unit_amount]','1495');form.set('line_items[0][price_data][product_data][name]',"MR GREY'S DESIRE — Kompletných 5 testov")}else form.set('line_items[0][price]',price);form.set('line_items[0][quantity]','1');form.set('metadata[app]','mrgreys_world');form.set('metadata[kind]',bundle?'desire_bundle':'desire_deep');if(!bundle)form.set('metadata[test_slug]',slug);form.set('metadata[member_email]',m.email);const r=await fetch('https://api.stripe.com/v1/checkout/sessions',{method:'POST',headers:{Authorization:`Bearer ${process.env.STRIPE_SECRET_KEY}`,'Content-Type':'application/x-www-form-urlencoded'},body:form.toString()});const d=await r.json();if(!r.ok||!d.url)throw new Error(d?.error?.message||'Stripe checkout failed');return NextResponse.redirect(d.url)}catch(e){return NextResponse.json({error:e.message||'Checkout failed'},{status:500})}
}
